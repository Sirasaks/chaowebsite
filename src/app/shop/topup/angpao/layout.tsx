import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'เติมเงิน TrueMoney (อั่งเปา)',
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
