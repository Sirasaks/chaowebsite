import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'เติมเงินผ่านธนาคาร',
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
